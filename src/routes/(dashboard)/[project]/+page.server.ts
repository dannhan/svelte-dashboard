// import { redirect } from '@sveltejs/kit';
// import type { PageServerLoad } from './[project]/$types';
//
// // todo
// export const load: PageServerLoad = async ({ params }) => {
//   // const { projects } = await parent();
//   // const project = projects.find((project) => project === params.project);
//   //
//   // if (params.project === 'placeholder') redirect(308, `${projects[0]}/identitas-proyek`);
//   // if (!project) throw error(404);;
//
//   redirect(308, `/${params.project}/identitas-proyek`);
// };
